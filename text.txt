python -m main/repl.py
python -m main.repl

CREATE TABLE users (id INT PRIMARY KEY, name TEXT);
CREATE TABLE orders (id INT PRIMARY KEY, user_id INT, amount INT);

INSERT INTO users VALUES 1 Walter
INSERT INTO orders VALUES 1 1 500

SELECT * FROM users JOIN orders ON users.id = orders.user_id

Update data
UPDATE users SET name=Kibet WHERE id=1

Verify UPDATE worked (via JOIN)
SELECT * FROM users JOIN orders ON users.id = orders.user_id
Expected:
{'id': 1, 'name': 'Kibet', 'user_id': 1, 'amount': 500}


Insert more data (to show indexing + multiple rows)
INSERT INTO users VALUES 2 Alice
INSERT INTO users VALUES 3 Bob

INSERT INTO orders VALUES 2 2 1200
INSERT INTO orders VALUES 3 3 300

JOIN with multiple rows
SELECT * FROM users JOIN orders ON users.id = orders.user_id
Expected:
{'id': 1, 'name': 'Kibet', 'user_id': 1, 'amount': 500}
{'id': 2, 'name': 'Alice', 'user_id': 2, 'amount': 1200}
{'id': 3, 'name': 'Bob', 'user_id': 3, 'amount': 300}

Delete a row
DELETE FROM orders WHERE id=1

Verify DELETE
SELECT * FROM users JOIN orders ON users.id = orders.user_id
Expected:
{'id': 2, 'name': 'Alice', 'user_id': 2, 'amount': 1200}
{'id': 3, 'name': 'Bob', 'user_id': 3, 'amount': 300}

Delete user (cascade not supported, intentional)
DELETE FROM users WHERE id=2


Final JOIN result
SELECT * FROM users JOIN orders ON users.id = orders.user_id
Expected:
{'id': 3, 'name': 'Bob', 'user_id': 3, 'amount': 300}

HOW TO EXPLAIN THIS IN ONE SENTENCE

“The REPL demonstrates a SQL-like interface over a custom in-memory relational engine supporting schema definition, joins, and full CRUD operations.”

